#include <stdio.h>
int main()
{
    // 10. [ Write a c program that calcuates the volume of a sphere ]
    float radius, volume;
    radius = 2.56;
    volume = (1.333333 * 3.141592 * 2.56 * 2.56 * 2.56);
    printf("Your Volume is : %f", volume);

    return 0;
}